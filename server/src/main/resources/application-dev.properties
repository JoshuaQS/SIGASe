# Perfil dev (spring.profiles.default=dev en application.properties).
# Crea o ajusta tablas según las entidades. No usar en producción: allí usa SPRING_JPA_HIBERNATE_DDL_AUTO=validate y migraciones.
spring.jpa.hibernate.ddl-auto=update
