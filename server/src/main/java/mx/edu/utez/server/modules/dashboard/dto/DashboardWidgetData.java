package mx.edu.utez.server.modules.dashboard.dto;

/**
 * Marker interface para payloads de widgets ya tipados.
 *
 * <p>Se mantiene compatibilidad de JSON, pero el backend deja de tratar el payload como anónimo
 * cuando la semántica del widget ya es estable.
 */
public interface DashboardWidgetData {
}
